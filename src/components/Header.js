import { Nav } from "./Nav";

const Header = (props) => {
  return (
    <header>
      <h1>Sartre's List</h1>
      <h2>Better-Dressed People</h2>
      <Nav refer="#" />
    </header>
  );
};

export default Header;